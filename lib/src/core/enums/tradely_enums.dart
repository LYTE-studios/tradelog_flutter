enum LongShortSelector {
  all,
  long,
  short,
}

enum ProFrequency {
  monthly,
  yearly,
}

enum AddTradeType {
  manual,
  file,
}

enum BuySellType {
  buy,
  sell,
}

enum PlanType {
  free,
  proMonthly,
  proYearly,
}

enum TradeType {
  long,
  short,
  both,
}

enum TradeStatus {
  closed,
  open,
  both,
}

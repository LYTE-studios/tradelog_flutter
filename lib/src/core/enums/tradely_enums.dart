enum LongShortSelector {
  all,
  long,
  short,
}

enum ProFrequency {
  monthly,
  yearly,
}

enum PlanType {
  free,
  proMonthly,
  proYearly,
}

enum TradeType {
  long,
  short,
  both,
}

enum TradeStatus {
  closed,
  open,
  both,
}

enum ProFrequency {
  monthly,
  yearly,
}

enum AddTradeType {
  manual,
  file,
}

enum PlanType {
  free,
  proMonthly,
  proYearly,
}

enum TradingPlatform {
  metaTrader4,
  metaTrader5,
  tradelockerDemo,
  tradelockerLive,
}
